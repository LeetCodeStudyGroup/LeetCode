class Solution {
public:
    int titleToNumber(string s) {
        int answer = 0;
        cout << s.size() << endl;
        for (int i=0; i<s.length(); i++) {
            answer = int(s[i] - 'A' + 1) + answer* 26;
        }
        
        return answer;
    }
};
